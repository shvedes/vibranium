You can easily set up QEMU virtual machines by going to *Vibranium Menu* (`CTRL ALT V`) -> *Setup* -> *QEMU*. The setup script installs all required packages and configures system services, networking, and related components.

After installation, you’ll receive a summary of what has been set up and be prompted whether to launch Virtual Machine Manager immediately.

![](qemu_complete.jpeg)