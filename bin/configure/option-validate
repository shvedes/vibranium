#!/usr/bin/env bash

VIBRANIUM_USER_SETTINGS="$XDG_CONFIG_HOME/vibranium/settings"

if [ -z "$1" ]; then
    exit 1
fi

if ! grep -q "^${1}=" "$VIBRANIUM_USER_SETTINGS"; then
    grep "^${1}=" "$VIBRANIUM_PATH/vb-core-defaults" >> "$VIBRANIUM_USER_SETTINGS"
fi
