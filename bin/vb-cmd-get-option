#!/usr/bin/env bash

source vb-core-lib

usage() {
    cat << EOF
Usage: ${0##*/} OPTION_NAME

Print the value of the specified Vibranium option.

Options:
  -h, --help  Display this help and exit
EOF
}

case "$1" in
    -h|--help)
        usage
        exit 0
        ;;
    "")
        echo "${0##*/}: missing OPTION_NAME" >&2
        echo "Try '${0##*/} --help' for more information." >&2
        exit 1
        ;;
esac

if [[ -n "${!1+x}" ]]; then
    printf '%s\n' "${!1}"
    exit 0
fi

echo "${0##*/}: option '$1' is not set" >&2
exit 1
