#!/usr/bin/env bash

if [[ -f "$XDG_RUNTIME_DIR/vibranium-core-force-kill.confirm" ]]; then
    printf '{"text": "%s", "tooltip": "%s"}' "\uee15" "Confirm force kill"
else
    printf '{"text": ""}'
fi
