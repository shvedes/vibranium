#!/bin/bash

source vb-lib-cli

usage() {
  shcat << EOF
Usage: ${0##*/} [OPTION]

Update all installed community themes.

Options:
  -h, --help     Display this help and exit
EOF
}

while [[ $# -gt 0 ]]; do
  case "$1" in
    -h | --help)
      usage
      exit 0
      ;;
    *)
      log Error "unknown option: $1"
      log Error "Try --help for more information"
      exit 1
      ;;
  esac
done

source vb-lib-core

_wait_for_key() {
  local color="${1:-$R}" msg="${2:-Operation canceled}"
  printf "%s[VIBRANIUM]%s %s" "$color" "$RS" "$msg"
  trap 'exit 1' SIGHUP SIGINT SIGTERM SIGQUIT
  term::disable_input

  # shellcheck disable=SC2034
  read -n1 -r -s -p $'. Press any key to exit' dummy
}

trap 'trap "exit 1" INT HUP; printf "\n"; _wait_for_key; exit 1' INT HUP

_spinner() {
  local pid=$1 msg="$2" delay=0.07
  # shellcheck disable=SC1003
  local spinstr=('/' '-' '\' '|')

  while kill -0 "$pid" 2> /dev/null; do
    for c in "${spinstr[@]}"; do
      printf '\r\033[K%s %s' "$msg" "$c"
      sleep $delay
    done
  done

  wait "$pid"
  return $?
}

_display_name() {
  local name="$1" word out=
  for word in ${name//-/ }; do
    out+="${word^} "
  done
  printf "%s" "${out% }"
}

term::show_header

THEMES_DIR="$XDG_CONFIG_HOME/vibranium/themes"
# Updated themes
UPD_THEMES=()
SKIPPED=0
UPTOD=0
FAILED=()

mapfile -t themes < <(
  find "$THEMES_DIR" -mindepth 1 -maxdepth 1 -type d -printf '%f\n' |
    while read -r folder; do
      if [[ ! -d "$VIBRANIUM/themes/$folder" ]]; then
        echo "$folder"
      fi
    done
)

if ((${#themes[@]} == 0)); then
  vb::print "$Y" "No community themes installed"
  _wait_for_key "$G" "No themes to update"
  exit 0
fi

mapfile -t themes < <(printf '%s\n' "${themes[@]}" | sort)

term::disable_input

for theme in "${themes[@]}"; do
  theme_path="$THEMES_DIR/$theme"
  display="${B}$(_display_name "$theme")${RS}"

  (sleep 0.1) &
  _spinner $! "${Y}[VIBRANIUM]${RS} ${Y}Checking${RS} $display"

  if [[ ! -d "$theme_path/.git" ]]; then
    ((SKIPPED++))
    continue
  fi

  if ! git -C "$theme_path" diff --quiet HEAD 2> /dev/null; then
    ((SKIPPED++))
    continue
  fi

  behind=$(git -C "$theme_path" rev-list --count 'HEAD..@{upstream}' 2> /dev/null)

  if [[ -z "$behind" ]] || ((behind == 0)); then
    ((UPTOD++))
    continue
  fi

  git -C "$theme_path" pull --ff-only &
  if ! _spinner $! "${Y}[VIBRANIUM]${RS} ${G}Updating${RS} $display"; then
    FAILED+=("$display")
    continue
  fi

  UPD_THEMES+=("$display")
done

printf '\r\033[K%s[VIBRANIUM]%s All themes processed\n' "$G" "$RS"

if ((${#UPD_THEMES[@]} > 0)); then
  vb::print "$G" "Updated themes:"
  for t in "${UPD_THEMES[@]}"; do
    echo -e "\t   - ${B}$t${RS}"
  done
fi

vb::print "$G" "Updated ${G}${#UPD_THEMES[@]}${RS} theme(s), ${C}$UPTOD${RS} already up to date, ${R}${#FAILED[@]}${RS} failed, ${B}$SKIPPED${RS} skipped"

if ((${#FAILED[@]} > 0)); then
  for failed in "${FAILED[@]}"; do
    echo "  ${R}-${RS} $failed"
  done
fi

_wait_for_key "$G" "All done"
