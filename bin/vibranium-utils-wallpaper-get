#!/usr/bin/env bash

HYRPPAPER_CONF="$XDG_CONFIG_HOME/hypr/hyprpaper.conf"

if [[ "$1" =~ ^(-b|--basename)$ ]]; then
    basename "$(sed -n 's/^wallpaper:path = //p' "$HYRPPAPER_CONF")"
else
    sed -n 's/^wallpaper:path = //p' "$HYRPPAPER_CONF"
fi
