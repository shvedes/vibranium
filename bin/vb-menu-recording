#!/usr/bin/env bash

if pidof -q gpu-screen-recorder; then
    vb-core-recording
    exit 0
fi

CHOSEN="$(printf "Screen\nRegion" | rofi -dmenu -sync -p "Record")"
[[ -z $CHOSEN ]] || [[ ! $CHOSEN =~ ^(Screen|Region)$ ]] && exit 1

exec setsid vb-core-recording "--${CHOSEN,,}"
