#!/usr/bin/env bash

# Copy-pasted from Omarchy.
# Intended to be launched by one of the udev rules.
# So no sudo for the iw command.

for iface in /sys/class/net/*/wireless; do
    iface="$(basename "$(dirname "$iface")")"
    iw dev "$iface" set power_save "$1" 2>/dev/null
done

