$attention = rgb({{ color9_strip }})

general {
  col.active_border = rgb({{ accent_strip }})
  col.inactive_border = rgb({{ color8_strip }})
}

misc {
  col.splash = rgb({{ background_strip }})
  background_color = rgb({{ background_strip }})
}
