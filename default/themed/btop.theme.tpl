theme[main_bg]="{{ background_0 }}"
theme[main_fg]="{{ foreground_0 }}"
theme[title]="{{ foreground_0 }}"
theme[hi_fg]="{{ accent }}"

theme[selected_bg]="{{ background_1 }}"
theme[selected_fg]="{{ accent }}"
theme[inactive_fg]="{{ gray }}"

theme[graph_text]="{{ gray|pop=0.10 }}"
theme[meter_bg]="{{ gray|dim=0.10 }}"
theme[proc_misc]="{{ foreground_0 }}"

theme[cpu_box]="{{ gray|dim=0.10 }}"
theme[mem_box]="{{ gray|dim=0.10 }}"
theme[net_box]="{{ gray|dim=0.10 }}"
theme[proc_box]="{{ gray|dim=0.10 }}"
theme[div_line]="{{ gray|dim=0.10 }}"

theme[temp_start]="{{ green }}"
theme[temp_mid]="{{ yellow }}"
theme[temp_end]="{{ red }}"

theme[cpu_start]="{{ cyan }}"
theme[cpu_mid]="{{ blue }}"
theme[cpu_end]="{{ purple }}"

theme[free_start]="{{ purple }}"
theme[free_mid]="{{ blue }}"
theme[free_end]="{{ cyan }}"

theme[cached_start]="{{ blue }}"
theme[cached_mid]="{{ cyan }}"
theme[cached_end]="{{ purple }}"

theme[available_start]="{{ yellow }}"
theme[available_mid]="{{ red }}"
theme[available_end]="{{ red }}"

theme[used_start]="{{ green }}"
theme[used_mid]="{{ cyan }}"
theme[used_end]="{{ blue }}"

theme[download_start]="{{ yellow }}"
theme[download_mid]="{{ red }}"
theme[download_end]="{{ red }}"

theme[upload_start]="{{ green }}"
theme[upload_mid]="{{ cyan }}"
theme[upload_end]="{{ blue }}"

theme[process_start]="{{ cyan }}"
theme[process_mid]="{{ blue }}"
theme[process_end]="{{ purple }}"
