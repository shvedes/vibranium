$attention = rgb({{ red_strip }})

general {
  col.active_border = rgba({{ accent_strip }}b3)
  col.inactive_border = rgb({{ background_3_strip }})
}

misc {
  col.splash = rgb({{ background_0_strip }})
  background_color = rgb({{ background_0_strip }})
}
