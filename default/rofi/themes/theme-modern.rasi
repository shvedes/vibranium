@import "../vibranium/theme/current/rofi.rasi"
@import "~/.local/share/vibranium/default/rofi/configuration.rasi"

* {
    background-color: transparent;
}

window {
    width:            350px;
    padding:          20;
    border:           2;
    border-color:     var(border);
    background-color: var(background);
}

element {
    padding: 10px;
    spacing: 0px 8px;
    cursor:  pointer;
}

element normal.normal {
    background-color: var(background);
    text-color:       var(foreground-muted);
}

element normal.active {
    background-color: var(border);
}

element selected.normal {
    background-color: var(border);
    text-color:       var(foreground);
}

element alternate.normal {
    background-color: var(background);
    text-color:       var(foreground-muted);
}

element-text {
    background-color: transparent;
    text-color:       inherit;
    cursor:           inherit;
}

element-icon {
    background-color: transparent;
}

listview {
    padding:      16px 0 0 0;
    border:       2px solid 0px 0px;
    border-color: var(highlight);
    margin:       12px 0 0 0;
	lines:        9;
}

scrollbar {
    padding:      1px 0px 0px 4px;
    handle-width: 2px;
    handle-color: var(highlight);
}

message {
    border: 0.15em;
    margin: 18px 2px 0px 2px;
    padding: 8px 8px 8px 8px;
    border-color: var(border);
}

entry {
    text-color:        var(foreground-muted);
    placeholder-color: var(foreground-muted);
    cursor:            text;
    placeholder:       "";
}

case-indicator {
    enabled: false;
}

num-filtered-rows {
    expand:     false;
    text-color: var(foreground-muted);
}

num-rows {
    expand:     false;
    text-color: var(border);
}

textbox-num-sep {
    expand:     false;
    str:        "/";
    text-color: var(border);
}

prompt {
    text-color: var(foreground);
}
